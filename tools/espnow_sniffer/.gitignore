# Generated by idf.py and target-specific. It also carries the OrbAP password
# once menuconfig has been run, so it must never be committed — this repo is
# published. Credentials live only in the local sdkconfig; sdkconfig.defaults
# (tracked) holds the non-secret defaults.
sdkconfig
sdkconfig.old
build/
